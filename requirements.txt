# Pinned with compatible-release bounds so a rebuild six months from now
# produces the same behaviour. The previous file listed bare package names,
# which meant pandas 2 and pandas 3 both satisfied it — and they differ in
# whether Series.astype(str) renders NaN as the string 'nan', which silently
# changed what landed in the Parquet lake.

# ── Core ─────────────────────────────────────────────────────────────────────
requests~=2.32
pandas~=3.0
numpy~=2.4
python-dotenv~=1.0

# ── Scheduling ───────────────────────────────────────────────────────────────
APScheduler~=3.11

# ── Quality control and contracts ────────────────────────────────────────────
scikit-learn~=1.9      # KNeighborsRegressor, used by spatial imputation
pandera~=0.33          # data contracts

# ── Storage and analytics ────────────────────────────────────────────────────
pyarrow~=21.0          # Parquet engine
duckdb~=1.3            # gold-layer aggregation over the Parquet lake

# ── GFS GRIB2 decoding ───────────────────────────────────────────────────────
# cfgrib wraps ECMWF's ecCodes. It needs the ecCodes SYSTEM library as well:
#   Debian/Ubuntu : sudo apt-get install -y libeccodes0
#   macOS         : brew install eccodes
#   conda         : conda install -c conda-forge eccodes
# Without it fetch_gfs raises a clear error instead of guessing at GRIB2
# packing, which is what the old hand-written parser did.
cfgrib~=0.9
xarray~=2025.1
eccodes~=2.38

# ── Optional: PostgreSQL backend (DB_ENGINE=postgres) ────────────────────────
# Uncomment if you are not using the default SQLite backend.
# psycopg2-binary~=2.9

# NOTE: sqlalchemy was listed here but never imported anywhere in the
# codebase. It has been removed rather than left as a phantom dependency.
